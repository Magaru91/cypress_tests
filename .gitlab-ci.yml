fase-de-pruebas-cypress:
  stage: test
  script:
    - echo "Instalando dependencias..."
    - npm install
    - echo "Verificando Cypress..."
    - npx cypress verify
    - echo "Ejecutando test: cypress/e2e/MisTAREAS/mis_tests.cy.js"
    - npx cypress run --spec "cypress/e2e/MisTAREAS/mis_tests.cy.js"
  artifacts:
    when: always
    paths:
      - cypress/screenshots/
      - cypress/videos/
    expire_in: 1 hour